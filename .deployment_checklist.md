# 🎯 Deployment Checklist - Virtual Portfolio Trading Verification

**Timer Set:** 20 minutes from now
**Goal:** Full pipeline working with virtual portfolio before real money trading

---

## ✅ DEPLOYMENT STATUS CHECKS:

### 1. Infrastructure Deployment
- [ ] Terraform apply succeeded
- [ ] Health check config updated (10s timeout, 5 unhealthy threshold)
- [ ] No terraform errors

**Command:**
```bash
cd infra && terraform plan -detailed-exitcode
```

### 2. Backend Deployment
- [ ] GitHub Actions build succeeded
- [ ] Docker image pushed to ECR
- [ ] App Runner deployment succeeded (not ROLLBACK)
- [ ] Current image is latest

**Commands:**
```bash
aws apprunner list-operations --service-arn "arn:aws:apprunner:eu-west-2:590183672693:service/tradepulse-backend/fc591a233e1c40f99a2768c95712abad" --region eu-west-2 --query 'OperationSummaryList[0]'

aws apprunner describe-service --service-arn "arn:aws:apprunner:eu-west-2:590183672693:service/tradepulse-backend/fc591a233e1c40f99a2768c95712abad" --region eu-west-2 --query 'Service.Status'
```

---

## 🧠 BRAIN CONTROLLER CHECKS:

### 3. Initialization Logs
- [ ] "BRAIN CONTROLLER: Starting initialization..."
- [ ] "BRAIN CONTROLLER: Initialization complete!"
- [ ] State shown (WARMUP/RUNNING)
- [ ] Auto-start countdown visible

**Command:**
```bash
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep "BRAIN CONTROLLER"
```

### 4. Operation Logs
- [ ] "BRAIN CYCLE: Monitoring trading engines status"
- [ ] "BRAIN: Day Trading Engine (Standard) operational"
- [ ] Cycle count increasing
- [ ] No errors in brain cycles

---

## 🎓 CONTINUOUS LEARNING CHECKS:

### 5. Initialization Logs
- [ ] "CONTINUOUS LEARNING ENGINE: Starting initialization..."
- [ ] "State loaded from DynamoDB"
- [ ] "Background tasks started"
- [ ] "Fully initialized!"
- [ ] Auto-optimization status shown

**Command:**
```bash
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep "CONTINUOUS LEARNING"
```

### 6. Operation Logs
- [ ] "Optimization loop STARTED"
- [ ] No errors accessing position_results table
- [ ] Parameters loaded (if any exist)

---

## 📊 SUPPORT/RESISTANCE CHECKS:

### 7. S/R Algorithm Status
- [ ] S/R DEBUG logs visible
- [ ] "Method 1 (historical) found X levels"
- [ ] "Method 2 (swing lows) found X levels"
- [ ] "Method 3 (Bollinger) added X levels"
- [ ] TOTAL S/R LEVELS > 0 (not 0!)

**Command:**
```bash
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep "S/R DEBUG"
```

---

## 💰 TRADING ACTIVITY CHECKS:

### 8. AI Signal Generation
- [ ] "AI signal generated: BUY/SELL with X% confidence"
- [ ] Confidence levels (should be 60%+)
- [ ] Signal frequency (every ~30-60 seconds)

### 9. Validator Decisions
- [ ] Validator running (not rejecting ALL signals)
- [ ] If rejecting, reasons should be valid (not "0 S/R")
- [ ] Some signals should pass validation

### 10. Position Management
- [ ] Positions opened (check for "Position opened" logs)
- [ ] Entry reasons shown
- [ ] Exit decisions being made
- [ ] PnL tracking visible

**Commands:**
```bash
# Check signals
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep "AI signal generated"

# Check validator
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep "DAY TRADING VALIDATOR"

# Check positions
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep -E "Position opened|Position closed"
```

---

## ❌ ERROR/WARNING CHECKS:

### 11. Critical Errors
- [ ] No "ResourceNotFoundException" errors
- [ ] No "Traceback" errors
- [ ] No "Exception" errors (excluding handled ones)
- [ ] No deployment rollback indicators

**Command:**
```bash
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep -E "ERROR|❌|Exception|Traceback" | head -50
```

### 12. Warnings to Review
- [ ] List all warnings
- [ ] Categorize (critical vs informational)
- [ ] Determine if any need immediate fix

**Command:**
```bash
aws logs tail "/aws/apprunner/tradepulse-backend/fc591a233e1c40f99a2768c95712abad/application" --region eu-west-2 --since 30m --format short | grep -E "WARNING|⚠️" | head -30
```

---

## 🎯 SUCCESS CRITERIA:

✅ **READY FOR REAL MONEY:**
1. Both deployments succeeded
2. Brain Controller running (cycles visible)
3. Continuous Learning initialized
4. S/R levels generating (> 0)
5. AI signals generating (60%+ confidence)
6. Validator passing some signals
7. Virtual portfolio trading (positions opening/closing)
8. No critical errors
9. Only informational warnings

❌ **NOT READY:**
- Any deployment failed
- Brain Controller not running
- Continuous Learning not initialized
- S/R still returning 0
- Validator blocking ALL signals
- No trading activity
- Critical errors present

---

## 📋 FINAL REPORT SECTIONS:

1. **Deployment Status** (PASS/FAIL)
2. **Brain Controller Status** (OPERATIONAL/DOWN)
3. **Continuous Learning Status** (ACTIVE/INACTIVE)
4. **Trading Pipeline Status** (TRADING/BLOCKED)
5. **Errors Summary** (count + severity)
6. **Warnings Summary** (count + categories)
7. **S/R Algorithm Status** (WORKING/BROKEN)
8. **Virtual Portfolio Activity** (trades count, PnL)
9. **Next Steps** (if issues found)
10. **Real Money Readiness** (YES/NO + reasons)

---

**Timer:** ⏰ 20 minutes
**Target Time:** Check deployment after sufficient time for health checks
**Expected Result:** Full green pipeline, virtual trading active, ready for real money! 💰
