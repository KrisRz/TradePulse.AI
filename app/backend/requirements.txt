# TradePulse.AI Backend Dependencies (Live-only)
# API + AI engines + Binance + DynamoDB Local client

# Web API
fastapi==0.115.7
uvicorn[standard]==0.33.0

# Config and validation (Pydantic v2)
pydantic[email]==2.11.7
pydantic-settings==2.10.1
email-validator==2.2.0

# Auth/security
PyJWT==2.10.1
python-jose[cryptography]==3.5.0
bcrypt==4.3.0

# Logging/monitoring
structlog==25.4.0
psutil==7.0.0
prometheus-client==0.21.0

# Async networking / HTTP
aiohttp==3.12.15
websockets==15.0.1
requests==2.31.0
tenacity==9.1.2

# AWS SDK (DynamoDB Local / AWS)
boto3==1.34.0
botocore==1.34.0

# AI/ML stack - Python 3.11 compatible versions 
numpy>=1.23.5,<2.0.0
scikit-learn==1.7.1
# Classical models may depend on gradient boosters - FIXED: Exact versions
xgboost==2.1.3
lightgbm==4.3.0
# TensorFlow STABLE VERSION - Python 3.11 compatible
tensorflow==2.16.1
# NOTE: Using official stable TensorFlow 2.16.1 from https://www.tensorflow.org/api_docs
# TensorFlow 2.20.0 had mutex deadlock - FIXED by using stable version
pandas>=2.2.3
pyarrow>=17.0.0
optuna>=3.6.1
# Technical Analysis implemented using pandas and numpy (no external dependencies)
# Kalman Filter: Custom implementation using NumPy (no additional dependencies)

# Env
python-dotenv==1.1.1

# Types
typing-extensions==4.14.1
